export class Player {
    public name: string;
    public avatar: string;
    public color: string;

    constructor(name: string, avatar: string, color: string) {
        this.name = name;
        this.avatar = avatar;
        this.color = color;
    }
}